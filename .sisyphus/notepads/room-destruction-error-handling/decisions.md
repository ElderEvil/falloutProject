## Error Handling Strategy
- Updated  in  to throw errors instead of just logging them.
- Added  to  in  to provide immediate visual feedback for failed destructions.
## Error Handling Strategy
- Updated `destroyRoom` in `roomStore` to throw errors instead of just logging them.
- Added `toast.error` to `handleDestroy` in `RoomDetailModal.vue` to provide immediate visual feedback for failed destructions.
