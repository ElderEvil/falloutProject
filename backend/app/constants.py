"""Game constants that are fundamental to the game world design.

These are hard-coded constraints that should not be configurable.
For game balance settings, use app.core.game_config instead.
"""

# Grid coordinate limits (fundamental to the vault map layout)
GRID_X_MIN = 0
GRID_X_MAX = 8
GRID_Y_MIN = 0
GRID_Y_MAX = 25
