import type { RouteRecordRaw } from 'vue-router'

// Note: Rooms don't have their own views - they're part of VaultView
// This routes file is for future expansion if room-specific views are added
export const roomsRoutes: RouteRecordRaw[] = []
